# CyberYozh Task Management System

Система управления задачами с возможностью создания, редактирования, назначения и комментирования задач, построенная на принципах Domain-Driven Design (DDD).

## Возможности

### Управление задачами
- ✅ Создание, редактирование и удаление задач
- 👥 Назначение задач другим пользователям
- ✔️ Отметка задач как выполненных
- 💬 Комментирование задач
- 📋 Пагинация списка задач

### Аутентификация и пользователи
- 🔐 JWT аутентификация
- 👤 Регистрация и управление профилем пользователей
- 🔑 Refresh токены для безопасности
- 🚪 Logout с blacklist токенов

### Документация и тестирование
- 📚 Автоматическая документация API (Swagger/OpenAPI)
- 🧪 Полное покрытие тестами (57 тестов)
- 🐳 Запуск через Docker
- 🛠️ Make команды для удобства разработки

## Технологический стек

- **Backend**: Django 4.2.7 + Django REST Framework
- **Authentication**: JWT (djangorestframework-simplejwt)
- **Database**: PostgreSQL 15
- **API Documentation**: drf-spectacular (Swagger/OpenAPI)
- **Testing**: Django TestCase + pytest
- **Containerization**: Docker + Docker Compose
- **Architecture**: Domain-Driven Design (DDD)

## Быстрый старт с Docker

### Предварительные требования

- Docker
- Docker Compose

### Запуск проекта

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd cyberyozh
   ```

2. **Запустите проект:**
   ```bash
   docker-compose up --build
   ```

3. **Проект будет доступен по адресам:**
   - **API**: http://localhost:8000/api/v1/
   - **Swagger UI**: http://localhost:8000/api/docs/
   - **ReDoc**: http://localhost:8000/api/redoc/
   - **Admin панель**: http://localhost:8000/admin/

4. **Данные для входа в админку:**
   - Логин: `admin`
   - Пароль: `admin123`

### Остановка проекта

```bash
docker-compose down
```

### Очистка данных

```bash
docker-compose down -v
```

## API Endpoints

### Аутентификация

- `POST /api/v1/auth/register/` - Регистрация нового пользователя
- `POST /api/v1/auth/login/` - Вход пользователя (получение JWT токенов)
- `POST /api/v1/auth/logout/` - Выход пользователя (blacklist refresh токена)
- `GET /api/v1/auth/profile/` - Получение профиля текущего пользователя
- `POST /api/v1/auth/token/refresh/` - Обновление access токена

### Задачи (Tasks)

- `GET /api/v1/tasks/` - Список всех задач (с пагинацией)
- `POST /api/v1/tasks/` - Создание новой задачи
- `GET /api/v1/tasks/{id}/` - Получение задачи по ID
- `PUT /api/v1/tasks/{id}/` - Полное обновление задачи
- `PATCH /api/v1/tasks/{id}/` - Частичное обновление задачи
- `DELETE /api/v1/tasks/{id}/` - Удаление задачи

### Специальные действия с задачами

- `PATCH /api/v1/tasks/{id}/assign/` - Назначение задачи пользователю
- `PATCH /api/v1/tasks/{id}/complete/` - Отметка задачи как выполненной

### Комментарии

- `GET /api/v1/tasks/{id}/comments/` - Получение комментариев к задаче
- `POST /api/v1/tasks/{id}/comments/` - Добавление комментария к задаче

## Разработка без Docker

### Предварительные требования

- Python 3.11+
- PostgreSQL

### Установка

1. **Создайте виртуальное окружение:**
   ```bash
   cd backend
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте базу данных:**
   ```bash
   # Создайте базу данных PostgreSQL
   createdb cyberyozh
   ```

4. **Выполните миграции:**
   ```bash
   python manage.py migrate
   ```

5. **Создайте суперпользователя:**
   ```bash
   python manage.py createsuperuser
   ```

6. **Запустите сервер разработки:**
   ```bash
   python manage.py runserver
   ```

## Тестирование

Проект имеет полное покрытие тестами с использованием Make команд:

```bash
# Запуск всех тестов
make test

# Запуск тестов пользователей
make test-users

# Запуск тестов задач  
make test-tasks

# Запуск unit тестов
make test-unit

# Запуск тестов репозиториев
make test-repositories

# Запуск E2E тестов API
make test-e2e
```

### Статус тестов
- **Users модуль**: ✅ 38/38 тестов проходят
- **Tasks модуль**: ✅ 19/19 тестов проходят
- **Общий статус**: ✅ Все тесты работают

## Структура проекта

```
cyberyozh/
├── backend/
│   ├── apps/
│   │   ├── tasks/
│   │   │   ├── domain/          # Доменная логика (сущности, сервисы)
│   │   │   ├── infrastructure/  # Репозитории, модели Django
│   │   │   ├── endpoints/       # API endpoints, сериализаторы
│   │   │   └── tests/           # Тесты модуля
│   │   └── users/
│   │       ├── domain/          # Доменная логика пользователей
│   │       ├── infrastructure/  # Репозитории пользователей
│   │       ├── endpoints/       # API аутентификации
│   │       └── tests/           # Тесты аутентификации
│   ├── config/
│   │   ├── settings/
│   │   │   ├── base.py         # Базовые настройки
│   │   │   ├── development.py  # Настройки для разработки
│   │   │   └── docker.py       # Настройки для Docker
│   │   └── urls.py
│   ├── Dockerfile
│   ├── entrypoint.sh
│   └── requirements.txt
├── Makefile                     # Make команды для разработки
├── docker-compose.yml
└── README.md
```

## Архитектура

Проект использует **Domain-Driven Design (DDD)** и **Clean Architecture** с разделением на слои:

### Слои архитектуры

- **Domain Layer**: 
  - Доменные сущности (`Task`, `User`, `TaskComment`)
  - Бизнес-сервисы (`TaskService`, `UserService`, `CommentService`)
  - Доменные исключения и интерфейсы

- **Infrastructure Layer**: 
  - Django модели (`TaskModel`, `User`)
  - Репозитории (`DjangoTaskRepository`, `DjangoUserRepository`)
  - JWT токены и аутентификация

- **Endpoints Layer**: 
  - API контроллеры (`TaskViewSet`, аутентификация views)
  - Сериализаторы для входных данных и ответов
  - OpenAPI документация

### Принципы

- **Инверсия зависимостей**: Доменный слой не зависит от инфраструктуры
- **Разделение ответственности**: Каждый слой имеет четкую роль
- **Тестируемость**: Полное покрытие unit и интеграционными тестами

## API Документация

После запуска проекта документация API доступна по адресам:

- **Swagger UI**: http://localhost:8000/api/docs/
- **ReDoc**: http://localhost:8000/api/redoc/
- **OpenAPI Schema**: http://localhost:8000/api/schema/

## Переменные окружения

Для Docker настройки задаются в `docker-compose.yml`:

- `DEBUG` - Режим отладки (1/0)
- `SECRET_KEY` - Секретный ключ Django
- `DATABASE_URL` - URL подключения к базе данных
- `DJANGO_SETTINGS_MODULE` - Модуль настроек Django

## Make команды для разработки

Проект включает удобные Make команды для разработки:

### Основные команды
```bash
make help          # Показать все доступные команды
make build         # Собрать Docker контейнеры
make up            # Запустить проект
make down          # Остановить проект
make restart       # Перезапустить проект
```

### Команды для работы с данными
```bash
make migrate       # Применить миграции
make makemigrations # Создать миграции
make superuser     # Создать суперпользователя
make shell         # Запустить Django shell
make collectstatic # Собрать статические файлы
```

### Команды тестирования
```bash
make test          # Запустить все тесты
make test-users    # Тесты пользователей
make test-tasks    # Тесты задач
make test-unit     # Unit тесты
make test-e2e      # E2E тесты API
make test-repositories # Тесты репозиториев
```

### Полезные команды Docker

```bash
# Пересборка контейнеров
docker-compose up --build

# Запуск в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f backend

# Выполнение команд в контейнере
docker-compose exec backend python manage.py shell

# Создание миграций
docker-compose exec backend python manage.py makemigrations

# Применение миграций
docker-compose exec backend python manage.py migrate
```

## Лицензия

MIT License
