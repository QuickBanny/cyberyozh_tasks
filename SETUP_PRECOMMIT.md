# Pre-commit Setup

Pre-commit hooks запускаются локально на вашей машине, а не в Docker контейнере.

## Установка

1. **Установите pre-commit локально:**
   ```bash
   pip install pre-commit
   ```

2. **Установите hooks в репозиторий:**
   ```bash
   make pre-commit-install
   # или напрямую:
   pre-commit install
   ```

3. **Проверьте работу на всех файлах:**
   ```bash
   make pre-commit-run
   # или напрямую:
   pre-commit run --all-files
   ```

## Использование

После установки pre-commit будет автоматически запускаться при каждом коммите и проверять:

- ✅ **Trailing whitespace** - удаление пробелов в конце строк
- ✅ **End of file fixer** - добавление пустой строки в конец файла
- ✅ **YAML check** - проверка синтаксиса YAML файлов
- ✅ **Large files check** - проверка на большие файлы
- ✅ **Merge conflict check** - проверка на конфликты слияния
- ✅ **Debug statements** - поиск debug statements
- 🎨 **Black** - форматирование Python кода
- 📦 **isort** - сортировка импортов
- 🔍 **flake8** - линтинг Python кода
- 🏷️ **mypy** - проверка типов

## Make команды

```bash
make pre-commit-install  # Установить hooks
make pre-commit-run      # Запустить на всех файлах
make pre-commit-update   # Обновить hooks
```

## Обход pre-commit (не рекомендуется)

Если нужно сделать коммит без проверок:
```bash
git commit --no-verify -m "commit message"
```

## Конфигурация

Настройки находятся в файле `.pre-commit-config.yaml` в корне проекта.
